<div class="animal_area_verticaltastic_wrapper">

<div class= "animal_area">
<div class="crazy_vert_align_hack_div" style="padding-bottom:<%= ((224 - winner.image.height(:medium)) / 2) %>px;">
</div>
<%= form_for battle, :remote => true, :html => { :'data-type' => 'html', :'class' => 'animal_image_form_real'} do |f| %>
  <div class = "animal_image_form">

  <% if battle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(battle.errors.count, "error") %> prohibited this battle from being saved:</h2>
  	   
      <ul>
      <% battle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.hidden_field :winner_id, :value=> winner.id %>
    <%= f.hidden_field :loser_id, :value=>  loser.id %>
    <%= f.hidden_field :match_id, :value => battle.match.id %>
	<div class="animal_image_wrapper">
	<%= image_submit_tag(winner.image.url(:medium), :value => winner.name, :class => "animal_image") %>
	</div>  
</div>
<% end %>

<div> <span> <%= winner.name %> </span> </div>
</div>
</div>

