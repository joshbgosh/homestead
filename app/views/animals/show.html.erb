<p id="notice"><%= notice %></p>

<p>
  <b><%= @animal.name %></b>
</p>

<p>
  <%= image_tag @animal.image.url(:medium)%>
</p>

<p> Wins  : <%= @animal.wins.count %> </p>
<p> Losses: <%= @animal.losses.count %> </p>


<% if @animal.arch_nemesis %>
<p> Arch-Nemesis: <%=  link_to @animal.arch_nemesis.name, @animal.arch_nemesis %> (defeated <%= @animal.name %> <%= pluralize(@animal.defeats_by(@animal.arch_nemesis).count, "time")%>!)</p>
<% else %>
<p> Arch-Nemesis: <%= @animal.name %> has no arch-nemesis. <%= @animal.name %> is UNDEFEATED! </p>
<% end%>

<% if @animal.dominates %>
<p> Dominates: <%= link_to @animal.dominates.name, @animal.dominates %> (<%= @animal.name%> has beaten <%= link_to @animal.dominates.name, @animal.dominates%> <%= pluralize(@animal.wins_against(@animal.dominates).count, "time") %>,
<% if @animal.defeats_by(@animal.dominates).count == 0 %>
and has never been defeated by it.) </p>
<% else %>
and has been defeated by it only <%= pluralize(@animal.defeats_by(@animal.dominates).count, "time")%>.) </p>
<% end %>

<% else %>
<p> Dominates: <%= @animal.name %> dominates no one. <%= @animal.name %> has never beaten anyone :( . </p>
<% end %>
<%= link_to 'Edit', edit_animal_path(@animal) %> |
<%= link_to 'Back', animals_path %>
